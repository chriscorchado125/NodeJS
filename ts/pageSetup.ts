/**
 * Update page
 * @param {string} page - page name
 */
const setupPage = (page: string) => {
  switch (page) {
    case 'about': // homepage
      console.log('test');
      break;
    case 'companies':
      break;
    case 'courses':
      break;
    case 'projects':
      break;
    case 'contact':
      break;
  }
};

module.exports = setupPage;
